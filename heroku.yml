setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE
  config:
    TZ: Asia/Hong_Kong

build:
  docker:
    web: Dockerfile

run:
  web: uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1
